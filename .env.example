# Copy this file to .env.local and fill in the values. See README.md for
# where to get each one. All optional — the site works without them, just
# with fallback contact options instead of live booking/email.

NEXT_PUBLIC_MYPTHUB_URL=
NEXT_PUBLIC_CALENDLY_URL=
NEXT_PUBLIC_WEB3FORMS_KEY=
